import { Route } from '@angular/router';
import { PaymentFlowComponent } from './view/payment-flow.component';

export const PaymentFlowRoutes: Route[] = [
    {
        path     : '',
        component: PaymentFlowComponent
    }
];
