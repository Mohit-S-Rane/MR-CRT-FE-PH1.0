import { Route } from '@angular/router';
import { CompleteReservationComponent } from './view/complete-reservation.component';

export const CompleteReservationRoutes: Route[] = [
    {
        path     : '',
        component: CompleteReservationComponent
    }
];
