import { Route } from '@angular/router';
import { ListRideComponent } from './view/list-ride.component';

export const ListRideRoutes: Route[] = [
    {
        path     : '',
        component: ListRideComponent
    }
];
